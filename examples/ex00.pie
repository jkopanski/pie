
'atom
( claim one ( -> Atom Atom ) )
(define one (\ (x) x))

( claim two (→  Atom Atom ) )
(define two (λ (x) x))
(one two three 'test)

; weird comments
(; coment1
a ; comment 2
; comment 3
b ; comment 4
) ; comment 5
